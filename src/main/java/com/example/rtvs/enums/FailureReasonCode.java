package com.example.rtvs.enums;

public enum FailureReasonCode {
    INSUFFICIENT_FUNDS,
    RECEIVER_NOT_FOUND,
    SENDER_NOT_FOUND,
    DAILY_LIMIT_EXCEEDED,
    DUPLICATE_REQUEST,
    INVALID_AMOUNT,
    ACCOUNT_BLOCKED
}
